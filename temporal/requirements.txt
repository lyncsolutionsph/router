fastapi>=0.70.0
uvicorn[standard]
pydantic>=1.8.0
aiofiles>=0.8.0
requests>=2.28.0
python-dotenv>=0.21.0
# Note: system-level tools used by the backend (nmcli, nft, dnsmasq, pihole)
# are installed via apt, not pip. See README for instructions.
